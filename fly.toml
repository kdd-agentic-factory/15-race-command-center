app = "kdd-rjz-race"
primary_region = "fra"

[build]
  dockerfile = "Dockerfile"

[http_service]
  internal_port = 8150
  force_https = true
  auto_stop_machines = "stop"
  auto_start_machines = true
  min_machines_running = 0

[checks.health]
  type = "http"
  port = 8150
  method = "get"
  path = "/health"
  interval = "30s"
  timeout = "5s"
  grace_period = "20s"

# Fly's managed Prometheus scrapes this → dashboards at fly-metrics.net (Grafana).
[metrics]
  port = 8150
  path = "/metrics"

[[vm]]
  memory = "512mb"
  cpu_kind = "shared"
  cpus = 1
