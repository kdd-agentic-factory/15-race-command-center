apiVersion: v1
kind: Service
metadata:
  name: race-command-center-dashboard
  namespace: race-command-center
spec:
  selector:
    app: race-command-center-dashboard
  ports:
    - name: http
      port: 80
      targetPort: 80

