apiVersion: v1
kind: Secret
metadata:
  name: race-command-center-secrets
  namespace: race-command-center
type: Opaque
stringData:
  POSTGRES_PASSWORD: "replace-me"
  MINIO_ROOT_PASSWORD: "replace-me"

